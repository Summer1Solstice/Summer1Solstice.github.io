#### 快速生成a-z  
通过序列迭代器生成字符编码再转为字符
```Python
# 生成小写英文字母列表（a-z 的 ASCII 码为 97-122）
lowercase_letters = [chr(i) for i in range(97, 123)]
print(lowercase_letters)

# 生成大写英文字母列表（A-Z 的 ASCII 码为 65-90）
uppercase_letters = [chr(i) for i in range(65, 91)]
print(uppercase_letters)
```
#### 使用按位运算优化
优化奇偶判断  
当最低位为1时为奇数
```AutoHotkey
if n & 1{
    return "odd"
}else{
    return "even"
}
```
#### 全角字符和半角字符的转换
全角字符与半角字符，差值是：`0XFEE0`  
全角字符的 Unicode 编码减去 `0XFEE0` 就是对应的半角字符，反之亦然。  
适用 ASCII ：`0X21-0X7E`，基本包含所有 ASCII 字符